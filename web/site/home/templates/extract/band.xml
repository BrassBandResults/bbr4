<band id="{{Band.id}}">
  <name>{{Band.name}}</name>
  <slug>{{Band.slug}}</slug>
  <website>{{Band.website}}</website>
  <twitter>{{Band.twitter_name}}</twitter>
  <region id="{{Band.region.id}}" slug="{{Band.region.slug}}">{{Band.region.name}}</region>
  <latitude>{{Band.latitude}}</latitude>
  <longitude>{{Band.longitude}}</longitude>
  <rehearsal>{{Band.get_rehearsal_night_1_display}}</rehearsal>
  <rehearsal>{{Band.get_rehearsal_night_2_display}}</rehearsal>
  {% if Band.first_parent %}<parent id="{{Band.first_parent.id}}" slug="{{Band.first_parent.slug}}">{{Band.first_parent.name}}</parent>{% endif %}
  {% if Band.second_parent %}<parent id="{{Band.second_parent.id}}"slug="{{Band.second_parent.slug}}">{{Band.second_parent.name}}</parent>{% endif %}
  <start>{{Band.start_date}}</start>
  <end>{{Bad.end_date}}</end>
  <status>{{Band.get_status_display}}</status>
  <grading>{{Band.national_grading}}</grading>
  <scratch_band>{{Band.scratch_band}}</scratch_band>
  <rehearsalNight1>{{Band.rehearsal_night_1}}</rehearsalNight1>
  <rehearsalNight2>{{Band.rehearsal_night_2}}</rehearsalNight2>
  <mapper>{{Band.mapper.username}}</mapper>
  <notes>{{Band.notes}}</notes>

  <lastModified>{{Band.last_modified}}</lastModified>
  <created>{{Band.created}}</created>
  <owner>{{Band.owner.username}}</owner>
  <lastChangedBy>{{Band.lastChangedBy.username}}</lastChangedBy>

  <previous_names>
    {% for name in Band.previous_band_names %}
    <previousName id="{{name.id}}">
      <name>{{name.old_name}}</name>
      <start>{{name.alias_start_date}}</start>
      <end>{{name.alias_end_date}}</end>
      <hidden>{{name.hidden}}</hidden>

      <lastModified>{{name.last_modified}}</lastModified>
      <created>{{name.created}}</created>
      <owner>{{name.owner.username}}</owner>
      <lastChangedBy>{{name.lastChangedBy.username}}</lastChangedBy>
    </previousName>
    {% endfor %}
  </previous_names>
</band>
